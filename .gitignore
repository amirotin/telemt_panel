# Binaries
/.container-bin/
/telemt-panel
/telemt-panel-lite
/telemt-mock
/panel
/panel-agent
/release/
/releases/
coverage.out
*.test

# Frontend
node_modules/
web/.openapi-filtered.yaml
web/.tanstack/
web/.vite/
web/coverage/

# Playwright e2e artifacts (web/e2e) — reports/traces from local runs.
web/playwright-report/
web/test-results/
web/blob-report/
# The §27.1 screenshot matrix (npm run screenshots). Deliberately NOT a
# committed baseline — see web/e2e/screenshots.ts for why the CI guard is
# the overflow sweep and the DOM assertions instead.
web/screenshots-out/

# Built SPA embedded by internal/webui (vite.config.ts outDir) — everything
# except the placeholder that lets `go build ./...` succeed before the
# first `make web` (see internal/webui/embed.go).
internal/webui/dist/*
!internal/webui/dist/.gitkeep

# Config with secrets
config.toml
.env
.env.*
!.env.example

# Local state
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite-wal
*.sqlite-shm

# IDE / tools
.claude/
.superpowers/
.codex/
.agents/
AGENTS.md
CLAUDE.md
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
